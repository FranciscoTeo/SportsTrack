import React from 'react';

// Componente placeholder para evitar erros de compilação
export const Subscription: React.FC = () => {
  return <div className="hidden">Subscription module disabled</div>;
};